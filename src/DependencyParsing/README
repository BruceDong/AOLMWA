1.Modify mutation of receptors of word agents.

Mutation based on rate of bit weigts

If the current affinity is 0 or ith weight w(i) is 0, then deta is assigned randomly.

If the current affinity is not 0 and ith weight w(i) is not 0, then

deta = w(i)/affinity;
w(i) = w(i) + deta;


